plugins {
    id "com.android.application"
    id "com.google.gms.google-services"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.chittoo"
    compileSdk = 34

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }
signingConfigs {
        debug {
            storeFile file(System.getenv("HOME") + "/.android/debug.keystore")
            storePassword "Asr12345@"  // This should match the password you set
            keyAlias "androiddebugkey"
            keyPassword "Asr12345@"  // This should match the password you set
        }
    }
    defaultConfig {
        applicationId "com.example.chittoo"
        minSdk = 23
        targetSdk = 34
        multiDexEnabled true
        versionCode 1
        versionName "1.0"
    }
 signingConfigs {
        debug {
            storeFile file("C:/Users/Amit/.android/debug.keystore") // Use the correct path
            storePassword "Asr12345@"
            keyAlias "androiddebugkey"
            keyPassword "Asr12345@"
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.debug
        }
    }
}

dependencies {
    implementation platform('com.google.firebase:firebase-bom:32.0.0')
    implementation 'com.google.firebase:firebase-auth'
    implementation 'com.google.android.gms:play-services-auth:20.5.0'
}

flutter {
    source = "../.."
}

apply plugin: 'com.google.gms.google-services'
